import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.articles',
  appName: 'Articles',
  webDir: 'www',
  server: {
    hostname: 'example.com',
  }
};

export default config;
